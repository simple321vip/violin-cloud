server:
  port: 8084
  servlet:
    context-path: /cloud
spring:
  profiles:
    active: prod
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update # 自动创建表以及更新表结构，生产环境慎用
    show-sql: true # 打印执行的SQL
